# Backend (society orchestrator) base URL the static bundle calls at runtime.
# Local dev  → http://localhost:8080 (default if this file is unset)
# Production → set to https://your-api-host (build will throw if http:// is used)
VITE_API_BASE=http://localhost:8080
