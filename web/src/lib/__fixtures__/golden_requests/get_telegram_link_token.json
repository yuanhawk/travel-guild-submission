{
  "endpoint": "getTelegramLinkToken",
  "method": "GET",
  "path": "/telegram/link",
  "kind": "query",
  "note": "Query-param endpoint — has NO request body. Required gate: user_id AND a session_token that verify_session() accepts. SECURITY FIX: session_token now rides as the X-Session-Token request header, not a query param (secrets-in-URL fix — bearer tokens must never land in access logs/browser history/Referer headers). This static fixture's session_token is a placeholder, NOT a live-issued token, so the backend contract test expects an honest 401 here (the field-presence gate passed and the token was actually checked).",
  "query": { "user_id": "demo-lena" },
  "headers": { "x-session-token": "gr-session-token" }
}
